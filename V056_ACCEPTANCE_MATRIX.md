# v0.5.6 — acceptance matrix (surface × state)

> Generated by `tools/gen_v056_matrix_table.py` from `tests/v056_matrix.py`,
> which is the same module `tests/test_v056_matrix.py` parametrizes over.
> Do not edit this file by hand: regenerate it.

**280 asserted cells · 56 N/A with a stated reason · 336 total**, of which 7 are labelled aliases and
31 additionally assert an executed equivalence against the
surface they wrap. Each asserted cell runs the surface in every output
format it supports and checks: the three axes present and mutually consistent
(`is_clean == not threat_found and inspection_complete`), exactly one document on
stdout (or `isError` for MCP), the HUMAN outcome line saying the same thing the
axes say, SARIF carrying `properties.inspectionComplete` whenever coverage is
partial, and no traceback on any path.

**What this table cannot prove, and what does.** A table generated from the
declarations proves the table and the tests read the same source. It cannot
prove the tests assert anything -- ASTRA demonstrated exactly that by replacing
one cell's human output with `CLEAN. All content was inspected.` and watching
the cell still pass. So `tests/test_v056_matrix.py` carries 18 MUTATION cases:
for every outcome and format, a response with its coverage evidence stripped is
fed to the same assertion functions the real cells use, and each must FAIL --
plus 8 unmutated controls that must PASS, so the mutation suite cannot be
satisfied by assertions that reject everything. No cell may skip: a grid-level
test asserts generated cells == declared non-N/A cells, exactly.

Outcome vocabulary — exit code and the axes it implies:

| outcome | exit | threat_found | inspection_complete | is_clean |
|---|---|---|---|---|
| `clean` | 0 | F | T | T |
| `threat` | 1 | T | T | F |
| `threat_incomplete` | 1 | T | F | F |
| `incomplete` | 3 | F | F | F |
| `operational` | 2 | — | — | F |

## States

| state | meaning |
|---|---|
| **clean** (`clean`) | ordinary content, fully readable |
| **finding** (`finding`) | a pattern fires, fully readable |
| **incomplete, no finding** (`incomplete_clean`) | part unread, nothing found in the rest |
| **incomplete + finding** (`incomplete_finding`) | a finding AND part of the input unread |
| **unreadable** (`unreadable`) | exists, cannot be opened (mode 000) |
| **missing** (`missing`) | path does not exist |
| **missing decoder** (`missing_dependency`) | readable media, no decoder installed |
| **truncated + finding** (`truncated_finding`) | over the 1 MiB cap, finding in the read part |
| **corrupt (parser fails)** (`corrupt_parser_fail`) | readable bytes whose format parser gives up |
| **not a regular file** (`nonregular`) | FIFO/socket/device/directory at the input path |
| **bytes do not decode** (`undecodable`) | a byte stream that is not valid UTF-8 |
| **empty** (`empty`) | input that legitimately carries no content |

## The matrix

| surface | clean | finding | incomplete, no finding | incomplete + finding | unreadable | missing | missing decoder | truncated + finding | corrupt (parser fails) | not a regular file | bytes do not decode | empty |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **CLI `scan --file`**<br>`human`, `json`, `sarif` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | 3 incomplete | 0 clean |
| **CLI `scan <path>`**<br>`human`, `json`, `sarif` | 0 clean ⇄ | 1 threat ⇄ | 3 incomplete ⇄ | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete ⇄ | 1 threat+incomplete | 3 incomplete ⇄ | 2 operational | 3 incomplete ⇄ | 0 clean ⇄ |
| **`sunglasses` console script**<br>`json` | 0 clean ⇄ | 1 threat ⇄ | 3 incomplete ⇄ | 1 threat+incomplete ⇄ | 2 operational ⇄ | 2 operational ⇄ | 3 incomplete ⇄ | 1 threat+incomplete ⇄ | 3 incomplete ⇄ | 2 operational ⇄ | 3 incomplete ⇄ | 0 clean ⇄ |
| **CLI `scan --text`**<br>`human`, `json`, `sarif` | 0 clean | 1 threat | N/A (host) | N/A (host) | N/A | N/A | N/A | N/A (host) | N/A | N/A | N/A | 0 clean |
| **CLI `scan --stdin`**<br>`human`, `json`, `sarif` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete ≡`truncated_finding` | N/A | N/A | N/A | 1 threat+incomplete | N/A | N/A | 2 operational | 0 clean |
| **CLI `scan --repo`**<br>`human`, `json`, `sarif` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 3 incomplete ⚠ | 2 operational | 3 incomplete | 3 incomplete ⚠ | 3 incomplete | N/A | 3 incomplete | 0 clean |
| **CLI `scan --deep`**<br>`human`, `json`, `sarif` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete ⚠ | 2 operational | 3 incomplete ≡`corrupt_parser_fail` | 0 clean |
| **`SunglassesEngine.scan()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete ≡`truncated_finding` | N/A | N/A | N/A | 1 threat+incomplete | N/A | N/A | N/A | 0 clean |
| **`SunglassesEngine.scan_file()`**<br>`dict` | 0 clean ⇄ | 1 threat ⇄ | 3 incomplete ⇄ | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete ⇄ | 1 threat+incomplete | 3 incomplete ⇄ | 2 operational | 3 incomplete ⇄ | 0 clean ⇄ |
| **`SunglassesScanner.scan_text()`**<br>`dict` | 0 clean ⇄ | 1 threat ⇄ | 3 incomplete ⇄ | 1 threat+incomplete ≡`truncated_finding` | N/A | N/A | N/A | 1 threat+incomplete ⇄ | N/A | N/A | N/A | 0 clean ⇄ |
| **`scan_fast()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | 3 incomplete | 0 clean |
| **`scan_auto(allow_deep=False)`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | 3 incomplete | 0 clean |
| **`scan_auto(allow_deep=True)`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | 3 incomplete | 0 clean |
| **`scan_email()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 3 incomplete ⚠ | 3 incomplete | 3 incomplete | 1 threat+incomplete | 3 incomplete | 3 incomplete | 3 incomplete | 0 clean |
| **`scan_deep()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | 3 incomplete ≡`corrupt_parser_fail` | 0 clean |
| **`_scan_image_fast()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | N/A | 3 incomplete ⚠ |
| **`_scan_pdf()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | N/A | 3 incomplete |
| **`_scan_text_file()`**<br>`dict` | 0 clean | 1 threat | N/A | 1 threat+incomplete ≡`truncated_finding` | 2 operational | 2 operational | N/A | 1 threat+incomplete | N/A | 2 operational | 3 incomplete | 0 clean |
| **`extractors.image.scan_image()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | N/A | 3 incomplete |
| **`extractors.pdf.scan_pdf()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | N/A | 3 incomplete |
| **`extractors.qr.scan_qr()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | N/A | 3 incomplete |
| **`extractors.audio.scan_audio()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | N/A | 3 incomplete |
| **`extractors.video.scan_video()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | N/A | 3 incomplete |
| **`result.normalize()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | N/A | N/A | 3 incomplete | 1 threat+incomplete | N/A | N/A | N/A | 3 incomplete ⚠ |
| **MCP `scan_text`**<br>`mcp` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete ≡`truncated_finding` | N/A | N/A | N/A | 1 threat+incomplete | N/A | N/A | N/A | 0 clean ⚠ |
| **MCP `scan_file` deep=false**<br>`mcp` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | 3 incomplete | 0 clean |
| **MCP `scan_file` deep=true**<br>`mcp` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete | 3 incomplete | 2 operational | 3 incomplete | 0 clean |
| **MCP over real stdio JSON-RPC**<br>`wire` | N/A | 1 threat | 3 incomplete | N/A | 2 operational | N/A | N/A | N/A | N/A | 2 operational | N/A | N/A |

## N/A cells, and why each one cannot exist

An omitted cell and a covered cell look identical in a table. These are the
cells the space does not contain, each with a claim a reviewer can check.

| surface | state | reason |
|---|---|---|
| CLI `scan --text` | `incomplete_clean` | an over-cap input cannot reach `--text`: this host's ARG_MAX is 1,048,576 bytes -- exactly the scan cap -- so execve() fails with E2BIG before the scanner runs. HOST-SCOPED: true of this machine, not of every OS or of a caller-configured cap. `--stdin` carries these two cells on every host **(HOST-SCOPED)** |
| CLI `scan --text` | `incomplete_finding` | an over-cap input cannot reach `--text`: this host's ARG_MAX is 1,048,576 bytes -- exactly the scan cap -- so execve() fails with E2BIG before the scanner runs. HOST-SCOPED: true of this machine, not of every OS or of a caller-configured cap. `--stdin` carries these two cells on every host **(HOST-SCOPED)** |
| CLI `scan --text` | `unreadable` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| CLI `scan --text` | `missing` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| CLI `scan --text` | `missing_dependency` | no extractor and no decoder runs on this surface; content arrives already extracted |
| CLI `scan --text` | `truncated_finding` | an over-cap input cannot reach `--text`: this host's ARG_MAX is 1,048,576 bytes -- exactly the scan cap -- so execve() fails with E2BIG before the scanner runs. HOST-SCOPED: true of this machine, not of every OS or of a caller-configured cap. `--stdin` carries these two cells on every host **(HOST-SCOPED)** |
| CLI `scan --text` | `corrupt_parser_fail` | no format parser runs on this surface: the content arrives as text and goes straight to the engine |
| CLI `scan --text` | `nonregular` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| CLI `scan --text` | `undecodable` | the parameter is already a `str`; byte-to-text decoding happened in the transport above this surface (see cli_stdin/undecodable) |
| CLI `scan --stdin` | `unreadable` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| CLI `scan --stdin` | `missing` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| CLI `scan --stdin` | `missing_dependency` | no extractor and no decoder runs on this surface; content arrives already extracted |
| CLI `scan --stdin` | `corrupt_parser_fail` | no format parser runs on this surface: the content arrives as text and goes straight to the engine |
| CLI `scan --stdin` | `nonregular` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| CLI `scan --repo` | `nonregular` | git's object model stores regular files, symlinks and directories only; a FIFO, socket or device node cannot be committed, so no clone can contain one. Verified by attempting `git add` on a FIFO fixture |
| `SunglassesEngine.scan()` | `unreadable` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| `SunglassesEngine.scan()` | `missing` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| `SunglassesEngine.scan()` | `missing_dependency` | no extractor and no decoder runs on this surface; content arrives already extracted |
| `SunglassesEngine.scan()` | `corrupt_parser_fail` | no format parser runs on this surface: the content arrives as text and goes straight to the engine |
| `SunglassesEngine.scan()` | `nonregular` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| `SunglassesEngine.scan()` | `undecodable` | the parameter is already a `str`; byte-to-text decoding happened in the transport above this surface (see cli_stdin/undecodable) |
| `SunglassesScanner.scan_text()` | `unreadable` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| `SunglassesScanner.scan_text()` | `missing` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| `SunglassesScanner.scan_text()` | `missing_dependency` | no extractor and no decoder runs on this surface; content arrives already extracted |
| `SunglassesScanner.scan_text()` | `corrupt_parser_fail` | no format parser runs on this surface: the content arrives as text and goes straight to the engine |
| `SunglassesScanner.scan_text()` | `nonregular` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| `SunglassesScanner.scan_text()` | `undecodable` | the parameter is already a `str`; byte-to-text decoding happened in the transport above this surface (see cli_stdin/undecodable) |
| `_scan_image_fast()` | `undecodable` | this helper is the per-format leg, selected by the caller, not a router: it is only reachable with a file of its own format, so the states that describe OTHER formats' failures cannot occur on it |
| `_scan_pdf()` | `undecodable` | this helper is the per-format leg, selected by the caller, not a router: it is only reachable with a file of its own format, so the states that describe OTHER formats' failures cannot occur on it |
| `_scan_text_file()` | `incomplete_clean` | this helper is the per-format leg, selected by the caller, not a router: it is only reachable with a file of its own format, so the states that describe OTHER formats' failures cannot occur on it |
| `_scan_text_file()` | `missing_dependency` | this helper is the per-format leg, selected by the caller, not a router: it is only reachable with a file of its own format, so the states that describe OTHER formats' failures cannot occur on it |
| `_scan_text_file()` | `corrupt_parser_fail` | this helper is the per-format leg, selected by the caller, not a router: it is only reachable with a file of its own format, so the states that describe OTHER formats' failures cannot occur on it |
| `extractors.image.scan_image()` | `undecodable` | a convenience function is the per-format leg its caller chose; it does no content sniffing and no routing, so states describing other formats cannot arise on it |
| `extractors.pdf.scan_pdf()` | `undecodable` | a convenience function is the per-format leg its caller chose; it does no content sniffing and no routing, so states describing other formats cannot arise on it |
| `extractors.qr.scan_qr()` | `undecodable` | a convenience function is the per-format leg its caller chose; it does no content sniffing and no routing, so states describing other formats cannot arise on it |
| `extractors.audio.scan_audio()` | `undecodable` | a convenience function is the per-format leg its caller chose; it does no content sniffing and no routing, so states describing other formats cannot arise on it |
| `extractors.video.scan_video()` | `undecodable` | a convenience function is the per-format leg its caller chose; it does no content sniffing and no routing, so states describing other formats cannot arise on it |
| `result.normalize()` | `unreadable` | `normalize()` takes an in-memory result object or mapping. It performs no I/O of any kind -- no path is resolved, no file opened, no bytes decoded, no parser run -- so every state defined by a filesystem or decoding failure reaches it only as a mapping that ALREADY records the failure, which is the `incomplete_*` cells |
| `result.normalize()` | `missing` | `normalize()` takes an in-memory result object or mapping. It performs no I/O of any kind -- no path is resolved, no file opened, no bytes decoded, no parser run -- so every state defined by a filesystem or decoding failure reaches it only as a mapping that ALREADY records the failure, which is the `incomplete_*` cells |
| `result.normalize()` | `corrupt_parser_fail` | `normalize()` takes an in-memory result object or mapping. It performs no I/O of any kind -- no path is resolved, no file opened, no bytes decoded, no parser run -- so every state defined by a filesystem or decoding failure reaches it only as a mapping that ALREADY records the failure, which is the `incomplete_*` cells |
| `result.normalize()` | `nonregular` | `normalize()` takes an in-memory result object or mapping. It performs no I/O of any kind -- no path is resolved, no file opened, no bytes decoded, no parser run -- so every state defined by a filesystem or decoding failure reaches it only as a mapping that ALREADY records the failure, which is the `incomplete_*` cells |
| `result.normalize()` | `undecodable` | `normalize()` takes an in-memory result object or mapping. It performs no I/O of any kind -- no path is resolved, no file opened, no bytes decoded, no parser run -- so every state defined by a filesystem or decoding failure reaches it only as a mapping that ALREADY records the failure, which is the `incomplete_*` cells |
| MCP `scan_text` | `unreadable` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| MCP `scan_text` | `missing` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| MCP `scan_text` | `missing_dependency` | no extractor and no decoder runs on this surface; content arrives already extracted |
| MCP `scan_text` | `corrupt_parser_fail` | no format parser runs on this surface: the content arrives as text and goes straight to the engine |
| MCP `scan_text` | `nonregular` | this surface's input parameter is a string of content; no path is resolved and no file is opened, so there is nothing to be missing, unreadable, non-regular or of the wrong file type |
| MCP `scan_text` | `undecodable` | the parameter is already a `str`; byte-to-text decoding happened in the transport above this surface (see cli_stdin/undecodable) |
| MCP over real stdio JSON-RPC | `clean` | the stdio leg exists to prove the TRANSPORT carries the document -- framing, Content-Length, JSON-RPC envelope, no stdout pollution. The verdict logic behind it is literally the same function object asserted in mcp_scan_file_*, and the four states below are the ones whose documents differ in SHAPE (error vs verdict, complete vs not), so they exercise every branch the transport has to carry |
| MCP over real stdio JSON-RPC | `incomplete_finding` | the stdio leg exists to prove the TRANSPORT carries the document -- framing, Content-Length, JSON-RPC envelope, no stdout pollution. The verdict logic behind it is literally the same function object asserted in mcp_scan_file_*, and the four states below are the ones whose documents differ in SHAPE (error vs verdict, complete vs not), so they exercise every branch the transport has to carry |
| MCP over real stdio JSON-RPC | `missing` | the stdio leg exists to prove the TRANSPORT carries the document -- framing, Content-Length, JSON-RPC envelope, no stdout pollution. The verdict logic behind it is literally the same function object asserted in mcp_scan_file_*, and the four states below are the ones whose documents differ in SHAPE (error vs verdict, complete vs not), so they exercise every branch the transport has to carry |
| MCP over real stdio JSON-RPC | `missing_dependency` | the stdio leg exists to prove the TRANSPORT carries the document -- framing, Content-Length, JSON-RPC envelope, no stdout pollution. The verdict logic behind it is literally the same function object asserted in mcp_scan_file_*, and the four states below are the ones whose documents differ in SHAPE (error vs verdict, complete vs not), so they exercise every branch the transport has to carry |
| MCP over real stdio JSON-RPC | `truncated_finding` | the stdio leg exists to prove the TRANSPORT carries the document -- framing, Content-Length, JSON-RPC envelope, no stdout pollution. The verdict logic behind it is literally the same function object asserted in mcp_scan_file_*, and the four states below are the ones whose documents differ in SHAPE (error vs verdict, complete vs not), so they exercise every branch the transport has to carry |
| MCP over real stdio JSON-RPC | `corrupt_parser_fail` | the stdio leg exists to prove the TRANSPORT carries the document -- framing, Content-Length, JSON-RPC envelope, no stdout pollution. The verdict logic behind it is literally the same function object asserted in mcp_scan_file_*, and the four states below are the ones whose documents differ in SHAPE (error vs verdict, complete vs not), so they exercise every branch the transport has to carry |
| MCP over real stdio JSON-RPC | `undecodable` | the stdio leg exists to prove the TRANSPORT carries the document -- framing, Content-Length, JSON-RPC envelope, no stdout pollution. The verdict logic behind it is literally the same function object asserted in mcp_scan_file_*, and the four states below are the ones whose documents differ in SHAPE (error vs verdict, complete vs not), so they exercise every branch the transport has to carry |
| MCP over real stdio JSON-RPC | `empty` | the stdio leg exists to prove the TRANSPORT carries the document -- framing, Content-Length, JSON-RPC envelope, no stdout pollution. The verdict logic behind it is literally the same function object asserted in mcp_scan_file_*, and the four states below are the ones whose documents differ in SHAPE (error vs verdict, complete vs not), so they exercise every branch the transport has to carry |

## Aliased cells — asserted, but not independent evidence

The state exists on this surface and the cell RUNS; its input is simply
the same input another state already describes. Labelling them stops the
count from reading as more coverage than it is.

| surface | state | driven as | why they are the same input |
|---|---|---|---|
| CLI `scan --stdin` | `incomplete_finding` | `truncated_finding` | on a string surface the only way to lose coverage is the size cap, so 'incomplete + finding' and 'truncated + finding' are the SAME input; asserted here under the alias rather than declared impossible |
| CLI `scan --deep` | `undecodable` | `corrupt_parser_fail` | on the deep path 'these bytes did not decode' IS the decoder-failure state -- both arrive through the same `_transcribe` except branch -- so it is asserted under that alias rather than declared impossible |
| `SunglassesEngine.scan()` | `incomplete_finding` | `truncated_finding` | on a string surface the only way to lose coverage is the size cap, so 'incomplete + finding' and 'truncated + finding' are the SAME input; asserted here under the alias rather than declared impossible |
| `SunglassesScanner.scan_text()` | `incomplete_finding` | `truncated_finding` | on a string surface the only way to lose coverage is the size cap, so 'incomplete + finding' and 'truncated + finding' are the SAME input; asserted here under the alias rather than declared impossible |
| `scan_deep()` | `undecodable` | `corrupt_parser_fail` | see cli_deep: the same `_transcribe` except branch |
| `_scan_text_file()` | `incomplete_finding` | `truncated_finding` | this helper runs no extractor, so the size cap is its only way to lose coverage -- same input as `truncated + finding` |
| MCP `scan_text` | `incomplete_finding` | `truncated_finding` | on a string surface the only way to lose coverage is the size cap, so 'incomplete + finding' and 'truncated + finding' are the SAME input; asserted here under the alias rather than declared impossible |

## Equivalences — proven by execution, not by name

ASTRA: *"Direct wrappers can use proven equivalence mappings; distinct
aggregates cannot inherit coverage by name."* For each row below the test
runs BOTH surfaces on the same input and requires the same exit code and
the same three axes. A wrapper that disagrees with what it wraps is a
second implementation, and this is how we find out.

| surface | state | must match |
|---|---|---|
| `sunglasses` console script | `clean` | CLI `scan --file` |
| `sunglasses` console script | `corrupt_parser_fail` | CLI `scan --file` |
| `sunglasses` console script | `empty` | CLI `scan --file` |
| `sunglasses` console script | `finding` | CLI `scan --file` |
| `sunglasses` console script | `incomplete_clean` | CLI `scan --file` |
| `sunglasses` console script | `incomplete_finding` | CLI `scan --file` |
| `sunglasses` console script | `missing` | CLI `scan --file` |
| `sunglasses` console script | `missing_dependency` | CLI `scan --file` |
| `sunglasses` console script | `nonregular` | CLI `scan --file` |
| `sunglasses` console script | `truncated_finding` | CLI `scan --file` |
| `sunglasses` console script | `undecodable` | CLI `scan --file` |
| `sunglasses` console script | `unreadable` | CLI `scan --file` |
| CLI `scan <path>` | `clean` | CLI `scan --file` |
| CLI `scan <path>` | `corrupt_parser_fail` | CLI `scan --file` |
| CLI `scan <path>` | `empty` | CLI `scan --file` |
| CLI `scan <path>` | `finding` | CLI `scan --file` |
| CLI `scan <path>` | `incomplete_clean` | CLI `scan --file` |
| CLI `scan <path>` | `missing_dependency` | CLI `scan --file` |
| CLI `scan <path>` | `undecodable` | CLI `scan --file` |
| `SunglassesEngine.scan_file()` | `clean` | `scan_fast()` |
| `SunglassesEngine.scan_file()` | `corrupt_parser_fail` | `scan_fast()` |
| `SunglassesEngine.scan_file()` | `empty` | `scan_fast()` |
| `SunglassesEngine.scan_file()` | `finding` | `scan_fast()` |
| `SunglassesEngine.scan_file()` | `incomplete_clean` | `scan_fast()` |
| `SunglassesEngine.scan_file()` | `missing_dependency` | `scan_fast()` |
| `SunglassesEngine.scan_file()` | `undecodable` | `scan_fast()` |
| `SunglassesScanner.scan_text()` | `clean` | `SunglassesEngine.scan()` |
| `SunglassesScanner.scan_text()` | `empty` | `SunglassesEngine.scan()` |
| `SunglassesScanner.scan_text()` | `finding` | `SunglassesEngine.scan()` |
| `SunglassesScanner.scan_text()` | `incomplete_clean` | `SunglassesEngine.scan()` |
| `SunglassesScanner.scan_text()` | `truncated_finding` | `SunglassesEngine.scan()` |

## Asserted cells that carry a caveat

Behaviour that is truthful but differs from a sibling surface. It is
recorded here rather than hidden in an N/A or asserted silently.

| surface | state | note |
|---|---|---|
| CLI `scan --repo` | `truncated_finding` | SURFACE DIVERGENCE, recorded not fixed. The repo walker SKIPS a file over the 1 MB limit and names it as not inspected (exit 3), where `--file` scans the first MiB and reports truncated + finding (exit 1). So an injection in the first KB of an oversized file is found by `--file` and missed by `--repo`. Both surfaces report their coverage honestly, which is why this is a gap and not a false clean. Unifying them changes what the walker reads and is v0.6 work; see KNOWN_VERSION_GAPS.md. ASTRA independently reproduced both halves and ratified the deferral. |
| CLI `scan --repo` | `unreadable` | The member is a committed symlink whose target is mode 000 -- ASTRA's counterexample to the round-3 N/A. The walker names it and the repo scan returns 3, NOT the operational 2 that `--file` returns for the same target: one unreadable member does not make a whole tree unscannable. |
| CLI `scan --deep` | `corrupt_parser_fail` | EXPLICIT EQUIVALENCE MAPPING, as ASTRA required. The decoder-failure fixture raises inside `AudioExtractor._transcribe`, the same except branch a real Whisper/ffmpeg failure lands in, and the assertion additionally requires the warning to NAME the decoder failure -- so it is distinguishable from the missing-decoder document, whose warning names the missing install. No successful Whisper run and no real Whisper corruption is claimed. |
| `scan_email()` | `unreadable` | DELIBERATE DIVERGENCE from the file surfaces. `scan_fast` raises operational on a mode-000 path; the email aggregate catches it per attachment, keeps scanning the body and the other attachments, and folds the failure in as lost coverage. An email is not unscannable because one of its parts is. |
| `_scan_image_fast()` | `empty` | DIVERGENCE from the text surfaces, and the right one: an empty TEXT input is content that was fully read, but an empty IMAGE file is a file whose image we could not decode. 0 bytes of text is complete; 0 bytes of PNG is incomplete. |
| `result.normalize()` | `empty` | INVERTED on purpose, and it is invariant 2. Everywhere else `empty` means '0 bytes of content, all of it read' -> clean. The analogous input HERE is a mapping that asserts nothing about coverage, and a document that never mentions `inspection_complete` did not prove an inspection. Silence is not a pass. The same test asserts the round-4 hardening: `normalize(None)` and `normalize(object())` raise TypeError rather than defaulting every axis to the optimistic value. |
| MCP `scan_text` | `empty` | An empty STRING is content and gets the clean 0-bytes document, matching the CLI. A MISSING `text` argument is a different thing -- the tool's API contract was broken and nothing was submitted -- and stays `isError: true`; the test asserts both, because `if not text` used to collapse them into one error. |

## Reproduce

```sh
python3 -m pytest tests/test_v056_matrix.py -q     # one test per asserted cell
python3 tools/gen_v056_matrix_table.py             # regenerates this file
```
