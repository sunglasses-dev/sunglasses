# v0.5.6 — acceptance matrix (surface × state)

> Generated by `tools/gen_v056_matrix_table.py` from `tests/v056_matrix.py`,
> which is the same module `tests/test_v056_matrix.py` parametrizes over.
> Do not edit this file by hand: regenerate it.

**87 asserted cells · 17 N/A with a stated reason · 104 total.** Each asserted cell runs the surface in every output
format it supports and checks: the three axes present and mutually consistent
(`is_clean == not threat_found and inspection_complete`), exactly one document on
stdout (or `isError` for MCP), SARIF carrying `properties.inspectionComplete`
whenever coverage is partial, and no traceback on any path.

Outcome vocabulary — exit code and the axes it implies:

| outcome | exit | threat_found | inspection_complete | is_clean |
|---|---|---|---|---|
| `clean` | 0 | F | T | T |
| `threat` | 1 | T | T | F |
| `threat_incomplete` | 1 | T | F | F |
| `incomplete` | 3 | F | F | F |
| `operational` | 2 | — | — | F |

## States

| state | meaning |
|---|---|
| **clean** (`clean`) | ordinary content, fully readable |
| **finding** (`finding`) | a pattern fires, fully readable |
| **incomplete, no finding** (`incomplete_clean`) | part unread, nothing found in the rest |
| **incomplete + finding** (`incomplete_finding`) | a finding AND part of the input unread |
| **unreadable** (`unreadable`) | exists, cannot be opened (mode 000) |
| **missing** (`missing`) | path does not exist |
| **missing decoder** (`missing_dependency`) | readable media, no decoder installed |
| **truncated + finding** (`truncated_finding`) | over the 1 MiB cap, finding in the read part |

## The matrix

| surface | clean | finding | incomplete, no finding | incomplete + finding | unreadable | missing | missing decoder | truncated + finding |
|---|---|---|---|---|---|---|---|---|
| **CLI `scan --file`**<br>`human`, `json`, `sarif` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete |
| **CLI `scan --text`**<br>`human`, `json`, `sarif` | 0 clean | 1 threat | N/A | N/A | N/A | N/A | N/A | N/A |
| **CLI `scan --stdin`**<br>`human`, `json`, `sarif` | 0 clean | 1 threat | 3 incomplete | N/A | N/A | N/A | N/A | 1 threat+incomplete |
| **CLI `scan --repo`**<br>`human`, `json`, `sarif` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | N/A | 2 operational | 3 incomplete | 3 incomplete ⚠ |
| **CLI `scan --deep`**<br>`human`, `json`, `sarif` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | N/A |
| **`scan_fast()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete |
| **`scan_auto(allow_deep=False)`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete |
| **`scan_auto(allow_deep=True)`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete |
| **`scan_email()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 3 incomplete | 3 incomplete | 3 incomplete | 1 threat+incomplete |
| **`scan_deep()`**<br>`dict` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | N/A |
| **MCP `scan_text`**<br>`mcp` | 0 clean | 1 threat | 3 incomplete | N/A | N/A | N/A | N/A | 1 threat+incomplete |
| **MCP `scan_file` deep=false**<br>`mcp` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete |
| **MCP `scan_file` deep=true**<br>`mcp` | 0 clean | 1 threat | 3 incomplete | 1 threat+incomplete | 2 operational | 2 operational | 3 incomplete | 1 threat+incomplete |

## N/A cells, and why each one cannot exist

An omitted cell and a covered cell look identical in a table. These are the
cells the space does not contain, each with a claim a reviewer can check.

| surface | state | reason |
|---|---|---|
| CLI `scan --text` | `incomplete_clean` | an over-cap input cannot reach `--text`: ARG_MAX is 1 MiB, exactly the scan cap, so the OS refuses the exec before the scanner sees it (verified: getconf ARG_MAX = 1048576). `--stdin` carries these two cells |
| CLI `scan --text` | `incomplete_finding` | a string surface's only incompleteness is the size cap; that cell is `truncated + finding` |
| CLI `scan --text` | `unreadable` | no file is opened on a string surface |
| CLI `scan --text` | `missing` | no path is resolved on a string surface |
| CLI `scan --text` | `missing_dependency` | no extractor runs on a string surface |
| CLI `scan --text` | `truncated_finding` | an over-cap input cannot reach `--text`: ARG_MAX is 1 MiB, exactly the scan cap, so the OS refuses the exec before the scanner sees it (verified: getconf ARG_MAX = 1048576). `--stdin` carries these two cells |
| CLI `scan --stdin` | `incomplete_finding` | a string surface's only incompleteness is the size cap; that cell is `truncated + finding` |
| CLI `scan --stdin` | `unreadable` | no file is opened on a string surface |
| CLI `scan --stdin` | `missing` | no path is resolved on a string surface |
| CLI `scan --stdin` | `missing_dependency` | no extractor runs on a string surface |
| CLI `scan --repo` | `unreadable` | `--repo` scans a fresh clone and git carries only the exec bit, so an unreadable member cannot survive the clone |
| CLI `scan --deep` | `truncated_finding` | the deep path scans a transcript produced under the cap; truncation is covered on the file and string surfaces |
| `scan_deep()` | `truncated_finding` | see cli_deep: the transcript is produced under the cap |
| MCP `scan_text` | `incomplete_finding` | a string surface's only incompleteness is the size cap; that cell is `truncated + finding` |
| MCP `scan_text` | `unreadable` | no file is opened on a string surface |
| MCP `scan_text` | `missing` | no path is resolved on a string surface |
| MCP `scan_text` | `missing_dependency` | no extractor runs on a string surface |

## Asserted cells that carry a caveat

Behaviour that is truthful but differs from a sibling surface. It is
recorded here rather than hidden in an N/A or asserted silently.

| surface | state | note |
|---|---|---|
| CLI `scan --repo` | `truncated_finding` | SURFACE DIVERGENCE, recorded not fixed. The repo walker SKIPS a file over the 1 MB limit and names it as not inspected (exit 3), where `--file` scans the first MiB and reports truncated + finding (exit 1). So an injection in the first KB of an oversized file is found by `--file` and missed by `--repo`. Both surfaces report their coverage honestly, which is why this is a gap and not a false clean. Unifying them changes what the walker reads and is v0.6 work, not a round-3 change; see KNOWN_VERSION_GAPS.md. |

## Reproduce

```sh
python3 -m pytest tests/test_v056_matrix.py -q     # one test per asserted cell
python3 tools/gen_v056_matrix_table.py             # regenerates this file
```
